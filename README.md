# Overport

The port is over 9000! This is a tiny Perl web-server that is HTTP/0.9 compliant.

## Dependencies

- A Perl interpreter

## Usage

```bash
./serve.pl (optional webroot)
```

* Please note: the webroot must have a file name `index.html` and the entry.